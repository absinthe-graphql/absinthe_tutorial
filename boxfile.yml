run.config:
  engine: elixir  

  engine.config:
    runtime: elixir-1.5

  engine.config:
    erlang_runtime: erlang-20.1

  dev_packages:
    - inotify-tools

data.db:
  image: nanobox/postgresql:9.6
